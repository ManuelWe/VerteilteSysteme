thread for every outputstream?
specify size of array on initialization?
handle socket timeouts
Checken ob auch text committed wurde bei uncommittedMEssages test

kill during election????

load file??

resend uncommitted entries (message in committed or uncommitted -> approve)

check on client if message is next sequence number or if messages are missing

client needs to check if he commits following sequence number; if he already has that sequence number committed -> replace existing